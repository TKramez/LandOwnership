name: LandOwnership
main: com.amity.landownership.LandOwnership
version: ${version}
depend: [Vault]

commands:
   plot:
      description: Controls for the LandOwnership plugin.
      usage: /plot <args>
      
permissions:
    land.admin:
        description: Gives full access to plot management commands.
        default: op
